stages:
  - build

update-images:
  image: docker:20.10
  stage: build
  services:
    - name: docker:20.10-dind
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - ./update-images.sh


